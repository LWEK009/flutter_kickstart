# flutter_kickstart


A Dart script that sets up a complete Flutter project in seconds — with folder structure, common packages, and **Cairo font** pre-configured.

## ✨ What it does

- Creates a new Flutter project with `flutter create`
- Sets up a clean folder structure (`core/`, `features/`, `shared/`)
- Installs common packages: `provider`, `dio`, `shared_preferences`, `go_router`, `flutter_svg`, `cached_network_image`, `google_fonts`
- Configures **Cairo** as the default font for the entire app (light & dark theme)
- Generates a ready-to-use `main.dart` and `app_theme.dart`

---

## ⚙️ Requirements

- [Flutter SDK](https://flutter.dev/docs/get-started/install) installed
- Dart (comes with Flutter automatically)

---

## 🐧 Linux
```bash
cd ~/Desktop
dart run flutter_setup.dart
```

## 🪟 Windows

Open **Command Prompt** or **PowerShell**, then:
```powershell
cd C:\Users\YourName\Desktop
dart run flutter_setup.dart
```

## 🍎 macOS
```bash
cd ~/Desktop
dart run flutter_setup.dart
```

---

## 📌 Usage

After running the script, answer the 2 questions:
